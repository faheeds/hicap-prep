node_modules/
.env
.env.*
!.env.example
.DS_Store
*.log
dist/
.claude/

# Local, per-developer Supabase runtime config — never commit real keys.
src/config.local.js

# Supabase CLI transient state — the CLI writes these when running locally.
supabase/.branches/
supabase/.temp/

.vercel
.env*
